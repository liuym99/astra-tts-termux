# chenxing model and reference audio

本目录对应 AstraTTS 的 `chenxing` V1 ONNX 模型及其示例参考音频。模型和参考音频均已获得发布许可。公开发布时请在仓库元数据或本文件中补充原始来源、作者、版本和完整许可证文本。

模型文件使用 Git LFS。克隆后执行 `git lfs install` 和 `git lfs pull`。`MODEL_MANIFEST.sha256` 用于校验模型和参考音频文件。参考音频位于 `references/`，仅作为该模型的示例资源。
